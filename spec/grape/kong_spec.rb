require 'spec_helper'

describe Grape::Kong do
  it 'has a version number' do
    expect(Grape::Kong::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
